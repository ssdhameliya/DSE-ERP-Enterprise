package org.example.controller;

import org.example.util.BusinessClock;

import org.example.api.support.SupportApiClient;
import org.example.util.OwnedAlert;
import org.example.util.OwnedTextInputDialog;
import java.util.List;
import org.example.util.IconFactory;
import org.example.util.TableSelectionSupport;
import org.example.util.SemanticTableCells;
import org.example.util.UiActionIcons;
import org.example.util.ScreenRefreshPolicy;
import org.example.navigation.ScreenLifecycle;
import org.example.util.FxDebouncer;
import org.example.util.UiTaskExecutor;
import org.example.util.PerformanceMonitor;
import org.example.util.InvoicePaymentDetailsDialog;
import org.example.util.RegisterPageState;
import org.example.util.RegisterUiSupport;
import org.example.util.AttachmentPreviewSupport;
import org.example.documentstudio.service.ExcelOutputService;
import javafx.print.PrinterJob;
import javafx.scene.Node;
import javafx.beans.property.*;import javafx.collections.FXCollections;import javafx.fxml.*;import javafx.geometry.Pos;import javafx.scene.Parent;import javafx.scene.control.*;import javafx.scene.layout.*;import javafx.stage.FileChooser;import org.apache.poi.ss.usermodel.Row;import org.apache.poi.ss.usermodel.Sheet;import org.apache.poi.ss.usermodel.Workbook;import org.apache.poi.xssf.usermodel.XSSFWorkbook;import org.example.model.Purchase;import org.example.navigation.NavigationManager;import org.example.service.*;import java.io.*;import java.nio.file.Files;
import java.nio.file.Path;import java.text.NumberFormat;import java.time.LocalDate;import java.util.*;
public class PurchaseListController implements ScreenLifecycle{
 public PurchaseListController(){}
 private Button btnSaveView; private MenuButton savedViewsMenu;
 @FXML private Label lblTotal,lblTotalCount,lblToday,lblTodayCount,lblPending,lblPendingCount,lblOverdue,lblOverdueCount,lblPaid,lblSummary,lblPageInfo,lblPageNumber,lblDetailInvoice,lblDetailSupplier,lblDetailContact,lblDetailAmount,lblDetailPaid,lblDetailBalance,lblDetailDue,lblDetailGst,lblDetailReference,lblDetailPaymentTerms,lblDetailContactPerson,lblDetailPhone,lblDetailEmail,lblDetailGstin,lblDetailCharges,lblDetailChargeTax,lblDetailGstType,lblDetailTransporter,lblDetailVehicle,lblDetailNotes,lblDetailAttachment,lblDetailBillingAddress,lblDetailDeliveryAddress;@FXML private Button btnNewPurchase,btnReset,btnRefresh,btnFirstPage,btnPreviousPage,btnNextPage,btnLastPage,btnExportExcel,btnExportPdf,btnAllDatesRange,btnTodayRange,btnYesterdayRange,btnSevenDaysRange,btnThirtyDaysRange,btnCustomRange,btnApprovePurchase,btnRejectPurchase;@FXML private TextField txtSearch;@FXML private ComboBox<String>cmbSupplier,cmbPaymentStatus,cmbMailStatus,cmbDocumentStatus,cmbReturnStatus;@FXML private ComboBox<Integer>cmbPageSize;@FXML private DatePicker dpFrom,dpTo;@FXML private ToggleButton btnAdvanced;@FXML private GridPane advancedFilters;@FXML private TableView<Purchase>tablePurchase;@FXML private TableColumn<Purchase,String>colInvoice,colDate,colSupplier,colMobile,colDue,colStatus,colPaymentStatus,colReturnStatus,colMail;@FXML private TableColumn<Purchase,Double>colAmount,colPaid,colBalance;@FXML private TableColumn<Purchase,Void>colActions;@FXML private SplitPane mainSplit;@FXML private VBox detailDrawer,approvalActionBox;@FXML private Button btnCloseDetails;@FXML private StackPane purchasePageIcon,purchaseHeaderSearchIcon,purchaseTotalIcon,purchaseOrdersIcon,purchaseSuppliersIcon,purchaseItemsIcon,purchasePaidIcon;
 private final PurchaseService service=new PurchaseService();private final SupportApiClient support=new SupportApiClient();private boolean explicitRefreshPending;private final FxDebouncer searchDebouncer=new FxDebouncer(java.time.Duration.ofMillis(220));private final NumberFormat money=NumberFormat.getCurrencyInstance(Locale.of("en", "IN"));private List<Purchase>all=List.of(),filtered=List.of();private Purchase selected;private boolean applyingSavedView;private boolean suppressFilterEvents;private String pendingSavedViewName;private final RegisterPageState pageState=new RegisterPageState();
 @FXML public void initialize(){
        org.example.shortcut.ShortcutRegistry.bindLabel(btnNewPurchase,org.example.shortcut.ShortcutRegistry.Action.NEW_PURCHASE,"Create Purchase");
        if(purchasePageIcon!=null)purchasePageIcon.getChildren().setAll(IconFactory.icon("purchase",24));RegisterUiSupport.configureHeaderSearch(txtSearch,purchaseHeaderSearchIcon,"Search invoice, supplier, mobile or GSTIN...");installKpiIcons();applyRangeButtonIcons();configureExplicitButtonIcons();configureDetailsCloseButton();decorateDetailDrawer();colInvoice.setCellValueFactory(v->new SimpleStringProperty(v.getValue().getInvoiceNo()));colDate.setCellValueFactory(v->new SimpleStringProperty(BusinessClock.formatDate(v.getValue().getInvoiceDate())));colSupplier.setCellValueFactory(v->new SimpleStringProperty(v.getValue().getSupplier().getName()));colMobile.setCellValueFactory(v->new SimpleStringProperty(s(v.getValue().getSupplier().getPhone())));colAmount.setCellValueFactory(v->new SimpleDoubleProperty(v.getValue().getTotalAmount()).asObject());colPaid.setCellValueFactory(v->new SimpleDoubleProperty(v.getValue().getPaidAmount()).asObject());colBalance.setCellValueFactory(v->new SimpleDoubleProperty(v.getValue().getBalanceAmount()).asObject());colDue.setCellValueFactory(v->new SimpleStringProperty(due(v.getValue())));colStatus.setCellValueFactory(v->new SimpleStringProperty(documentStatus(v.getValue())));colPaymentStatus.setCellValueFactory(v->new SimpleStringProperty(paymentStatusDisplay(v.getValue())));if(colReturnStatus!=null)colReturnStatus.setCellValueFactory(v->new SimpleStringProperty(v.getValue()==null?"N/A":v.getValue().getReturnStatus()));colMail.setCellValueFactory(v->new SimpleStringProperty(v.getValue().isEmailSent()?"Sent":"Not Sent"));colAmount.setCellFactory(x->totalMoneyCell());colPaid.setCellFactory(x->paidMoneyCell());colBalance.setCellFactory(x->balanceMoneyCell());colStatus.setCellFactory(x->SemanticTableCells.status("document"));colPaymentStatus.setCellFactory(x->SemanticTableCells.status("payment"));if(colReturnStatus!=null)colReturnStatus.setCellFactory(x->SemanticTableCells.status("return"));colMail.setCellFactory(x->pill());setupActions();cmbPaymentStatus.getItems().setAll("All","PENDING","PARTIAL","PAID","OVERDUE");cmbPaymentStatus.setValue("All");cmbMailStatus.getItems().setAll("All","Sent","Not Sent");cmbMailStatus.setValue("All");cmbDocumentStatus.getItems().setAll("All","DRAFT","PENDING APPROVAL","APPROVED","REJECTED","CANCELLED");cmbDocumentStatus.setValue("All");cmbReturnStatus.getItems().setAll("All","N/A","PENDING APPROVAL","PARTIALLY RETURNED","FULLY RETURNED");cmbReturnStatus.setValue("All");org.example.util.PartySearchUi.install(cmbSupplier,"SUPPLIER","All Suppliers","purchase-register-supplier-search");cmbPageSize.getItems().setAll(10,25,50,100);cmbPageSize.setValue(25);cmbPageSize.valueProperty().addListener((o,a,b)->{pageState.reset();reloadPage(false);});dpFrom.setValue(BusinessClock.today().minusMonths(6));dpTo.setValue(BusinessClock.today());if(advancedFilters!=null){advancedFilters.setVisible(false);advancedFilters.setManaged(false);}if(btnAdvanced!=null){btnAdvanced.setVisible(false);btnAdvanced.setManaged(false);}if(btnCustomRange!=null){btnCustomRange.setVisible(false);btnCustomRange.setManaged(false);}txtSearch.textProperty().addListener((o,a,b)->{if(!filterEventsSuppressed())searchDebouncer.submit(this::filter);});cmbSupplier.valueProperty().addListener((o,a,b)->{if(!filterEventsSuppressed()&&!org.example.util.PartySearchUi.isInternalUpdate(cmbSupplier))filter();});cmbPaymentStatus.valueProperty().addListener((o,a,b)->{if(!filterEventsSuppressed())filter();});dpFrom.valueProperty().addListener((o,a,b)->{if(!filterEventsSuppressed())filter();});dpTo.valueProperty().addListener((o,a,b)->{if(!filterEventsSuppressed())filter();});cmbMailStatus.valueProperty().addListener((o,a,b)->{if(!filterEventsSuppressed())filter();});cmbDocumentStatus.valueProperty().addListener((o,a,b)->{if(!filterEventsSuppressed())filter();});cmbReturnStatus.valueProperty().addListener((o,a,b)->{if(!filterEventsSuppressed())filter();});RegisterUiSupport.hideDrawer(detailDrawer,mainSplit,tablePurchase);org.example.util.OperationalUiSupport.installEscapeClose(mainSplit,()->detailDrawer!=null&&detailDrawer.isVisible(),this::closeDetails);org.example.util.OperationalUiSupport.focusWorkArea(tablePurchase);refresh();configureModernTable();installRegisterTools();org.example.util.RegisterColumnPreferences.install(tablePurchase,"PURCHASE_REGISTER");}


 private void decorateDetailDrawer(){
  if(detailDrawer==null)return;
  drawerValue(lblDetailInvoice,"document");drawerValue(lblDetailSupplier,"supplier");drawerValue(lblDetailContact,"user");
  decoratePurchaseDrawerCaptions(detailDrawer);
 }
 private void decoratePurchaseDrawerCaptions(Node node){
  if(node instanceof Label label&&label.getGraphic()==null){String semantic=drawerSemantic(label.getText());if(semantic!=null){label.setGraphic(IconFactory.compactIcon(semantic,14));label.setGraphicTextGap(6);label.getStyleClass().add("erp-drawer-caption");IconFactory.applySemanticLabelColour(label,semantic);label.getProperties().put("erp-icon-preserve",true);}}
  if(node instanceof ButtonBase button&&button.getGraphic()==null){String semantic=drawerSemantic(button.getText());if(semantic!=null)UiActionIcons.apply(button,semantic);}
  if(node instanceof Parent parent)for(Node child:parent.getChildrenUnmodifiable())decoratePurchaseDrawerCaptions(child);
 }
 private void drawerValue(Label label,String semantic){if(label==null)return;label.setGraphic(IconFactory.compactIcon(semantic,14));label.setGraphicTextGap(7);label.getStyleClass().add("erp-drawer-value");label.getProperties().put("erp-icon-preserve",true);}
 private String drawerSemantic(String text){String t=s(text).trim().toLowerCase(java.util.Locale.ROOT);if(t.equals("purchase details")||t.contains("invoice")||t.contains("reference"))return"document";if(t.contains("supplier"))return"supplier";if(t.contains("contact"))return"user";if(t.contains("phone")||t.contains("mobile"))return"phone";if(t.contains("email"))return"email";if(t.contains("gst")||t.contains("tax"))return"tax";if(t.contains("balance"))return"balance";if(t.contains("paid")||t.contains("payment"))return"payment";if(t.contains("amount")||t.contains("charge"))return"currency";if(t.contains("due"))return"calendar";if(t.contains("transport")||t.contains("vehicle"))return"delivery";if(t.contains("note"))return"notes";if(t.contains("attachment"))return"attachment";if(t.contains("address"))return"location";if(t.contains("excel"))return"excel";if(t.contains("pdf")||t.contains("print"))return"pdf";if(t.contains("close"))return"cancel";return null;}

 private void installKpiIcons(){
  setKpiIcon(purchaseTotalIcon,"purchase");setKpiIcon(purchaseOrdersIcon,"document");setKpiIcon(purchaseSuppliersIcon,"supplier");setKpiIcon(purchaseItemsIcon,"item");setKpiIcon(purchasePaidIcon,"payment");
 }
 private void setKpiIcon(StackPane pane,String semantic){if(pane!=null)pane.getChildren().setAll(IconFactory.compactIcon(semantic,22));}

 private TableCell<Purchase,Double> moneyCell(){return new TableCell<>(){protected void updateItem(Double v,boolean e){super.updateItem(v,e);setText(e||v==null?null:fmt(v));setAlignment(Pos.CENTER_RIGHT);}};}
 private TableCell<Purchase,Double> totalMoneyCell(){return coloredMoneyCell("erp-table-value-colour-pink","erp-table-value-colour-pink");}
 private TableCell<Purchase,Double> balanceMoneyCell(){return coloredMoneyCell("erp-table-value-colour-blue","erp-table-value-colour-green");}
 private TableCell<Purchase,Double> coloredMoneyCell(String positiveClass,String zeroClass){return new TableCell<>(){protected void updateItem(Double v,boolean e){super.updateItem(v,e);setText(e||v==null?null:fmt(v));setAlignment(Pos.CENTER_RIGHT);getStyleClass().removeIf(style->style!=null&&style.startsWith("erp-table-value-colour-"));if(!e&&v!=null){String style=v>.009?positiveClass:zeroClass;if(style!=null)getStyleClass().add(style);}}};}
 private TableCell<Purchase,Double> paidMoneyCell(){return new TableCell<>(){protected void updateItem(Double v,boolean e){super.updateItem(v,e);setText(e||v==null?null:fmt(v));setAlignment(Pos.CENTER_RIGHT);getStyleClass().removeIf(style->style!=null&&style.startsWith("erp-table-value-colour-"));if(!e&&v!=null)getStyleClass().add(v>.009?"erp-table-value-colour-green":"erp-table-value-colour-pink");}};}
 private void configureExplicitButtonIcons(){
  setButtonIcon(btnNewPurchase,"purchase");setButtonIcon(btnReset,"reset");setButtonIcon(btnRefresh,"refresh");
  setButtonIcon(btnFirstPage,"first");setButtonIcon(btnPreviousPage,"previous");setButtonIcon(btnNextPage,"next");setButtonIcon(btnLastPage,"last");
  setButtonIcon(btnExportExcel,"excel");setButtonIcon(btnExportPdf,"pdf");
 }
 private void setButtonIcon(ButtonBase button,String semantic){UiActionIcons.apply(button, semantic);}
 private void configureModernTable(){
 colStatus.setText("Doc Status");colStatus.setCellValueFactory(v->new SimpleStringProperty(documentStatus(v.getValue())));
 colStatus.setCellFactory(x->SemanticTableCells.status("document"));colPaymentStatus.setCellFactory(x->SemanticTableCells.status("payment"));if(colReturnStatus!=null)colReturnStatus.setCellFactory(x->SemanticTableCells.status("return"));colMail.setCellFactory(x->SemanticTableCells.status("email"));colDue.setCellFactory(x->SemanticTableCells.dueDate());
  tablePurchase.setRowFactory(tv->{TableRow<Purchase> row=new TableRow<>();row.setOnMouseClicked(e->{if(row.isEmpty()||e.getButton()!=javafx.scene.input.MouseButton.PRIMARY||e.getClickCount()!=1||RegisterUiSupport.isInteractiveTableTarget(e.getPickResult().getIntersectedNode(),row))return;Purchase clicked=row.getItem();if(detailDrawer.isVisible()&&selected!=null&&selected.getId()==clicked.getId())closeDetails();else{tablePurchase.getSelectionModel().select(clicked);details(clicked);}e.consume();});return row;});
  // Apply the resize policy after every dynamic column has been installed. This
  // makes the register consume the complete available width in both themes.
 }
 private void showPaymentDetails(Purchase purchase){try{InvoicePaymentDetailsDialog.show(tablePurchase,support,"PURCHASE",purchase.getId(),purchase.getInvoiceNo(),"Supplier",purchase.getSupplier()==null?"":purchase.getSupplier().getName(),purchase.getTotalAmount(),purchase.getPaidAmount(),purchase.getBalanceAmount());}catch(Exception e){error(e);}}
 private TableCell<Purchase,String>purchaseDueCell(){return new TableCell<>(){@Override protected void updateItem(String value,boolean empty){super.updateItem(value,empty);setText(empty?null:value);setGraphic(null);getStyleClass().removeAll("pill-success","pill-warning","pill-danger");if(!empty&&value!=null){boolean paid=value.startsWith("Paid"),overdue=value.startsWith("Overdue");getStyleClass().add(paid?"pill-success":overdue?"pill-danger":"pill-warning");setGraphic(IconFactory.statusIcon(overdue?"error":paid?"save":"reminder",overdue?"danger":paid?"success":"info"));}}};}
 private TableCell<Purchase,String>pill(){return pill("document");}private TableCell<Purchase,String>pill(String icon){return new TableCell<>(){protected void updateItem(String v,boolean e){super.updateItem(v,e);setText(e?null:v);setGraphic(null);getStyleClass().removeAll("pill-success","pill-warning","pill-danger");if(!e&&v!=null){boolean returned="RETURNED".equalsIgnoreCase(v),partialReturn="PARTIALLY RETURNED".equalsIgnoreCase(v),good="COMPLETED".equals(v)||"Sent".equals(v)||returned,pending="IN PROGRESS".equals(v)||"PENDING".equals(v)||"PENDING APPROVAL".equalsIgnoreCase(v)||partialReturn;getStyleClass().add(good?"pill-success":pending?"pill-warning":"pill-danger");setGraphic(IconFactory.statusIcon(returned||partialReturn?"return":good?icon:pending?("document".equals(icon)?"reminder":icon):"error",good?"success":pending?"info":"danger"));}}};}
 private String documentStatus(Purchase p){String stored=s(p==null?null:p.getDocumentStatus()).trim().toUpperCase(java.util.Locale.ROOT);return stored.isBlank()?"PENDING APPROVAL":stored;}
 private void installRegisterTools(){Node parent=tablePurchase.getParent();if(!(parent instanceof VBox box))return;HBox bar=null;for(Node n:box.getChildren())if(n instanceof HBox h&&h.getStyleClass().contains("export-bar")){bar=h;break;}if(bar==null||bar.getProperties().putIfAbsent("tools-installed",true)!=null)return;Button print=new Button("Print");print.setGraphic(IconFactory.icon("print"));print.getStyleClass().add("secondary-button");print.setOnAction(e->printRegister());bar.getChildren().add(Math.min(2,bar.getChildren().size()),print);}
 private void printRegister(){PrinterJob job=PrinterJob.createPrinterJob();if(job!=null&&job.showPrintDialog(tablePurchase.getScene().getWindow())){if(job.printPage(tablePurchase))job.endJob();}}
 private void setupActions(){
  colActions.setCellFactory(c->new TableCell<>(){
   final MenuButton m=new MenuButton();
   final MenuItem edit;
   final MenuItem payment;
   final MenuItem createReturn;
   final MenuItem cancel;
   final MenuItem delete;
   {
    m.getProperties().put("erp.icon.semantic","actions");m.setGraphic(IconFactory.compactIcon("actions",15));
    item("View Purchase","view",e->view(row()));
    item("Activity Timeline","history",e->org.example.util.ActivityTimelineDialog.show(tablePurchase,"PURCHASE",row().getId(),row().getInvoiceNo()));
    edit=item("Edit Purchase","edit",e->edit(row()));
    item("Duplicate Purchase","copy",e->duplicate(row()));
    item("Preview / Download PDF","print",e->pdf(row()));
    item("View / Download Excel","excel",e->excel(row()));
    item("Send Email","email",e->email(row()));
    item("Send WhatsApp","whatsapp",e->whatsapp(row()));
    payment=item("View / Record Payments","payment",e->payment(row()));
    createReturn=item("Create Purchase Return","return",e->createReturn(row()));
    item("Notes / Remarks","notes",e->notes(row()));
    cancel=item("Cancel Purchase","cancel",e->cancelPurchase(row()));
    delete=item("Delete Purchase","delete",e->delete(row()));
    delete.getStyleClass().add("danger-menu-item");
    m.setOnShowing(e->updateAvailability());
    m.getStyleClass().add("row-actions");m.setGraphic(IconFactory.compactIcon("actions",16));m.setText("Actions");m.setContentDisplay(ContentDisplay.LEFT);m.setGraphicTextGap(6);m.setTooltip(new Tooltip("Actions"));IconFactory.decorateActionMenu(m);
   }
   private void updateAvailability(){
    Purchase current=getTableRow()==null?null:getTableRow().getItem();
    if(current==null){edit.setDisable(true);payment.setDisable(true);createReturn.setDisable(true);cancel.setDisable(true);delete.setDisable(true);return;}
    String status=s(current.getDocumentStatus()).trim().toUpperCase(java.util.Locale.ROOT);
    boolean cancelled="CANCELLED".equals(status),deleted="DELETED".equals(status),inactive=cancelled||deleted;
    boolean locked=isFinanciallyLocked(current);
    edit.setDisable(inactive);
    payment.setDisable(inactive||"DRAFT".equals(status)||isApprovalLocked(current));
    createReturn.setDisable(!isReturnEligible(current));
    cancel.setDisable(locked||inactive);
    delete.setDisable(locked||deleted);
    cancel.setVisible(true);delete.setVisible(true);
   }
   private Purchase row(){Purchase value=getTableRow()==null?null:getTableRow().getItem();if(value==null)throw new IllegalStateException("This purchase row is no longer available. Refresh the register and try again.");return value;}
   private MenuItem item(String n,String icon,javafx.event.EventHandler<javafx.event.ActionEvent>h){MenuItem i=new MenuItem(n);i.getProperties().put("erp.icon.semantic",icon);i.setGraphic(IconFactory.compactIcon(icon,16));i.setOnAction(event->{try{h.handle(event);}catch(Throwable failure){error(failure);}});m.getItems().add(i);return i;}
   @Override protected void updateItem(Void v,boolean e){super.updateItem(v,e);setGraphic(e?null:m);setAlignment(Pos.CENTER);}
  });
 }

 @FXML public void refresh(){reloadPage(true);}
 @FXML private void refreshWithFeedback(){explicitRefreshPending=true;if(btnRefresh!=null){btnRefresh.setDisable(true);btnRefresh.setText("Refreshing...");}reloadPage(true);}
 private void finishExplicitRefresh(boolean success){if(!explicitRefreshPending)return;explicitRefreshPending=false;if(btnRefresh!=null){btnRefresh.setDisable(false);btnRefresh.setText("Refresh");}if(success)org.example.util.ToastManager.info(tablePurchase,"Refreshed","Purchase Register is up to date.");}
 private void reloadPage(boolean includeSummary){btnRefresh.setDisable(true);org.example.util.OperationalUiSupport.showLoading(tablePurchase,"Loading purchases…");int requested=pageState.currentPage(),size=cmbPageSize.getValue()==null?25:cmbPageSize.getValue();String supplier=cmbSupplier.getValue();if(supplier!=null&&supplier.startsWith("All"))supplier="";String selectedSupplier=supplier;UiTaskExecutor.submitLatest("purchase-register-load",()->service.page(requested,size,txtSearch.getText(),selectedSupplier,dpFrom.getValue(),dpTo.getValue(),cmbPaymentStatus.getValue(),"All",cmbMailStatus.getValue(),cmbDocumentStatus.getValue(),cmbReturnStatus.getValue(),includeSummary),this::applyPurchasePage,failure->{pendingSavedViewName=null;btnRefresh.setDisable(false);finishExplicitRefresh(false);org.example.util.OperationalUiSupport.showError(tablePurchase,"Purchase register could not load",failure);error(failure);});}
 private void applyPurchasePage(org.example.api.operations.OperationsApiClient.PurchasePage loaded){pageState.runApplying(()->{long started=System.nanoTime();all=loaded.rows()==null?List.of():List.copyOf(loaded.rows());filtered=all;pageState.apply(loaded.page(),loaded.totalPages(),loaded.totalRows());String selectedSupplier=cmbSupplier.getValue();org.example.util.PartySearchUi.preserveSelection(cmbSupplier,selectedSupplier,"All Suppliers");renderPage();applyMetrics(loaded.metrics());if(loaded.metrics()!=null)lblTotalCount.setText(loaded.metrics().activeDocuments()+" active • "+loaded.totalRows()+" records");applySummary(loaded.filteredTotals());openLinkedRecordIfRequested();ScreenRefreshPolicy.markRefreshed("purchase-register");btnRefresh.setDisable(false);finishExplicitRefresh(true);notifyAppliedSavedView();long ms=(System.nanoTime()-started)/1_000_000L;PerformanceMonitor.event("controller-phase","purchase-register-page-apply | "+ms+" ms | rows="+all.size()+" | total="+pageState.totalRows());});}
 private void openLinkedRecordIfRequested(){LinkedRecordContext.Target target=LinkedRecordContext.consume("PURCHASE");if(target==null)return;Purchase purchase=all.stream().filter(x->(target.recordId()!=null&&x.getId()==target.recordId())||(!target.documentNo().isBlank()&&target.documentNo().equalsIgnoreCase(s(x.getInvoiceNo())))).findFirst().orElse(null);if(purchase!=null){tablePurchase.getSelectionModel().select(purchase);tablePurchase.scrollTo(purchase);org.example.navigation.DeepLinkSupport.pulse(tablePurchase);details(purchase);PerformanceMonitor.event("linked-navigation","PURCHASE -> "+purchase.getInvoiceNo()+" | source="+target.source());return;}if(target.documentNo().isBlank()){info("The linked Purchase is not on the current result page. Use Search to locate it.");return;}UiTaskExecutor.submitLatest("purchase-linked-record",()->service.getByInvoice(target.documentNo()),found->{if(found==null){info("The linked Purchase is no longer available: "+target.documentNo());return;}details(found);PerformanceMonitor.event("linked-navigation","PURCHASE -> "+found.getInvoiceNo()+" | source="+target.source());},this::error);}
 private void applyMetrics(org.example.api.operations.OperationsApiClient.PurchaseMetrics m){if(m==null)return;lblTotal.setText(fmt(m.totalPurchases()));lblTotalCount.setText(m.activeDocuments()+" active");lblToday.setText(String.valueOf(m.activeDocuments()));lblTodayCount.setText("Accounting-active orders");lblPending.setText(String.valueOf(m.suppliers()));lblPendingCount.setText("Suppliers");lblOverdue.setText(String.valueOf((long)m.itemQuantity()));lblOverdueCount.setText("Items");lblPaid.setText(fmt(m.paidAmount()));}
 private void filter(){if(filterEventsSuppressed())return;pageState.runWhenIdle(()->{if(filterEventsSuppressed())return;pageState.reset();reloadPage(true);});}
 private void applySummary(org.example.api.operations.OperationsApiClient.RegisterTotals totals){if(totals==null){lblSummary.setText(pageState.totalRows()+" purchases");return;}lblSummary.setText(pageState.totalRows()+" purchases • Active Total "+fmt(totals.total())+" • Active Pending "+fmt(totals.balance()));}
 private void renderPage(){tablePurchase.setItems(FXCollections.observableArrayList(all));if(all.isEmpty())org.example.util.OperationalUiSupport.showEmpty(tablePurchase,"No purchases found","Adjust the filters or create a new Purchase.");int size=cmbPageSize.getValue()==null?25:cmbPageSize.getValue();RegisterUiSupport.updatePageLabels(pageState,lblPageInfo,lblPageNumber,size,all.size(),"entries");if(pageState.totalRows()==0)lblPageInfo.setText("No entries");}
 @FXML private void firstPage(){if(pageState.first())reloadPage(false);}@FXML private void previousPage(){if(pageState.previous())reloadPage(false);}@FXML private void nextPage(){if(pageState.next())reloadPage(false);}@FXML private void lastPage(){if(pageState.last())reloadPage(false);}
 @FXML private void showAllDates(){batchFilterUpdate(()->{dpFrom.setValue(null);dpTo.setValue(null);});filter();}
 @FXML private void showToday(){applyDateRange(BusinessClock.today(),BusinessClock.today());}
 @FXML private void showYesterday(){LocalDate d=BusinessClock.today().minusDays(1);applyDateRange(d,d);}
 @FXML private void showSevenDays(){applyDateRange(BusinessClock.today().minusDays(6),BusinessClock.today());}
 @FXML private void showThirtyDays(){applyDateRange(BusinessClock.today().minusDays(29),BusinessClock.today());}
 @FXML private void showCustomRange(){dpFrom.requestFocus();}
 private void applyDateRange(LocalDate from,LocalDate to){batchFilterUpdate(()->{dpFrom.setValue(from);dpTo.setValue(to);});filter();}
 @FXML private void saveCurrentView(){
  TextInputDialog d=new OwnedTextInputDialog();d.setTitle("Save Filter View");d.setHeaderText("Save the current purchase filters");d.setContentText("View name:");
  d.showAndWait().map(String::trim).filter(x->!x.isBlank()).ifPresent(name->{
   String data=String.join("|",s(cmbSupplier.getValue()),str(dpFrom.getValue()),str(dpTo.getValue()),s(cmbPaymentStatus.getValue()),s(cmbMailStatus.getValue()),s(cmbDocumentStatus.getValue()),s(txtSearch.getText()),s(cmbReturnStatus.getValue()));
   Integer uid=SessionService.current()==null?null:SessionService.current().getId();
   UiTaskExecutor.submitAction("purchase-save-view",()->{support.saveView(uid,"PURCHASE_REGISTER",name,data);return true;},ignored->{org.example.util.ToastManager.success(tablePurchase,"Saved view created",name);},failure->error(failure instanceof Exception e?e:new RuntimeException(failure)));
  });
 }
 private MenuItem savedViewPlaceholder(){MenuItem i=new MenuItem("No saved views");i.getStyleClass().add("register-saved-view-item");i.setDisable(true);return i;}
 private void loadSavedViews(){if(savedViewsMenu==null)return;Integer uid=SessionService.current()==null?null:SessionService.current().getId();UiTaskExecutor.submitLatest("purchase-saved-views",()->support.savedViews("PURCHASE_REGISTER",uid),views->{savedViewsMenu.getItems().clear();for(SupportApiClient.SavedView v:views){MenuItem i=new MenuItem(v.name());i.getStyleClass().add("register-saved-view-item");i.setOnAction(e->applySaved(v.name(),v.data()));savedViewsMenu.getItems().add(i);}if(savedViewsMenu.getItems().isEmpty())savedViewsMenu.getItems().add(savedViewPlaceholder());},failure->{savedViewsMenu.getItems().setAll(savedViewPlaceholder());PerformanceMonitor.event("purchase-saved-views",String.valueOf(failure.getMessage()));});}
 private void applySaved(String name,String data){String[]x=data==null?new String[0]:data.split("\\|",-1);if(x.length==0){info("The saved view is empty and cannot be applied.");return;}searchDebouncer.cancel();applyingSavedView=true;try{String supplier=part(x,0);org.example.util.PartySearchUi.preserveSelection(cmbSupplier,supplier.isBlank()?"All Suppliers":supplier,"All Suppliers");dpFrom.setValue(parseDate(part(x,1)));dpTo.setValue(parseDate(part(x,2)));selectSaved(cmbPaymentStatus,part(x,3),"All");selectSaved(cmbMailStatus,part(x,4),"All");selectSaved(cmbDocumentStatus,part(x,5),"All");txtSearch.setText(part(x,6));selectSaved(cmbReturnStatus,part(x,7),"All");pageState.reset();pendingSavedViewName=name;}finally{applyingSavedView=false;}searchDebouncer.cancel();reloadPage(true);}
 private void notifyAppliedSavedView(){if(pendingSavedViewName==null)return;String name=pendingSavedViewName;pendingSavedViewName=null;org.example.util.ToastManager.info(tablePurchase,"Saved view applied",name+" filters are now active.");}
 private static String part(String[] values,int index){return values!=null&&index>=0&&index<values.length&&values[index]!=null?values[index]:"";}
 private static void selectSaved(ComboBox<String> box,String raw,String fallback){if(box==null)return;String value=raw==null||raw.isBlank()?fallback:raw.trim();String match=box.getItems().stream().filter(v->v!=null&&v.equalsIgnoreCase(value)).findFirst().orElse(null);if(match==null&&!value.equalsIgnoreCase(fallback)){box.getItems().add(value);match=value;}box.setValue(match!=null?match:fallback);}
 private String str(LocalDate d){return d==null?"":d.toString();}
 private LocalDate parseDate(String d){try{return d==null||d.isBlank()?null:LocalDate.parse(d);}catch(Exception ignored){return null;}}
 @FXML private void toggleAdvanced(){advancedFilters.setManaged(btnAdvanced.isSelected());advancedFilters.setVisible(btnAdvanced.isSelected());}@FXML private void resetFilters(){batchFilterUpdate(()->{txtSearch.clear();cmbSupplier.setValue("All Suppliers");cmbPaymentStatus.setValue("All");cmbMailStatus.setValue("All");cmbDocumentStatus.setValue("All");cmbReturnStatus.setValue("All");dpFrom.setValue(BusinessClock.today().minusMonths(6));dpTo.setValue(BusinessClock.today());});searchDebouncer.cancel();filter();}
 @FXML private void newPurchase(){NavigationManager.navigateOrReport("/fxml/pages/Purchase.fxml");}
 private void edit(Purchase p){openPurchaseEditor(p,false);}
 private void view(Purchase p){openPurchaseEditor(p,true);}
 private void openPurchaseEditor(Purchase p,boolean viewOnly){
  try{
   FXMLLoader loader=new FXMLLoader(org.example.util.ResourceLocator.require("/fxml/pages/Purchase.fxml"));
   Parent root=loader.load();
   org.example.util.ProfessionalUiEnhancer.enhance(root);
   PurchaseController controller=loader.getController();
   Purchase full=service.getByInvoice(p.getInvoiceNo());
   if(full==null)throw new IllegalStateException("Purchase invoice "+p.getInvoiceNo()+" was not found. Refresh the register and try again.");
   controller.loadPurchase(full);
   if(viewOnly)controller.setViewMode(true);
   NavigationManager.getInstance().showPreparedPage("/fxml/pages/Purchase.fxml",root,controller);
  }catch(Exception e){error(e);}
 }
 private void details(Purchase p){selected=p;RegisterUiSupport.showDrawer(detailDrawer,mainSplit,.8);lblDetailInvoice.setText(p.getInvoiceNo());lblDetailSupplier.setText(p.getSupplier()==null?"Unknown Supplier":p.getSupplier().getName());lblDetailContact.setText(p.getSupplier()==null?"":s(p.getSupplier().getPhone())+"\n"+s(p.getSupplier().getEmail())+"\n"+s(p.getSupplier().getGstin()));lblDetailAmount.setText(fmt(p.getTotalAmount()));lblDetailPaid.setText(fmt(p.getPaidAmount()));lblDetailBalance.setText(fmt(p.getBalanceAmount()));lblDetailDue.setText(due(p));if(lblDetailGst!=null)lblDetailGst.setText(fmt(p.getGstAmount()));if(lblDetailReference!=null)lblDetailReference.setText(s(p.getReferenceNo()).isBlank()?"Not set":p.getReferenceNo());if(lblDetailPaymentTerms!=null)lblDetailPaymentTerms.setText(s(p.getPaymentTerms()).isBlank()?"Not set":p.getPaymentTerms());if(lblDetailContactPerson!=null)lblDetailContactPerson.setText(s(p.getContactPerson()).isBlank()?(p.getSupplier()==null||s(p.getSupplier().getContactPerson()).isBlank()?"Not set":p.getSupplier().getContactPerson()):p.getContactPerson());if(lblDetailPhone!=null)lblDetailPhone.setText(p.getSupplier()==null||s(p.getSupplier().getPhone()).isBlank()?"Not set":p.getSupplier().getPhone());if(lblDetailEmail!=null)lblDetailEmail.setText(p.getSupplier()==null||s(p.getSupplier().getEmail()).isBlank()?"Not set":p.getSupplier().getEmail());if(lblDetailGstin!=null)lblDetailGstin.setText(s(p.getBillingGstin()).isBlank()?(p.getSupplier()==null||s(p.getSupplier().getGstin()).isBlank()?"Not set":p.getSupplier().getGstin()):p.getBillingGstin());if(lblDetailCharges!=null)lblDetailCharges.setText(p.getCharges()==null||p.getCharges().isEmpty()?"Not Applicable":p.getCharges().stream().map(c->c.getChargeType()+" • "+fmt(c.getAmount())+(c.isTaxable()?" (GST "+String.format(java.util.Locale.ROOT,"%.2f%%",c.getGstPercent())+")":"")).collect(java.util.stream.Collectors.joining("\n")));if(lblDetailChargeTax!=null)lblDetailChargeTax.setText(fmt(p.getChargesTaxAmount()));if(lblDetailGstType!=null)lblDetailGstType.setText(s(p.getGstType()).isBlank()?"Not Applicable":p.getGstType());if(lblDetailTransporter!=null)lblDetailTransporter.setText(s(p.getTransporter()).isBlank()?"Not Applicable":p.getTransporter());if(lblDetailVehicle!=null)lblDetailVehicle.setText(s(p.getVehicleNumber()).isBlank()?"Not Applicable":p.getVehicleNumber());if(lblDetailNotes!=null)lblDetailNotes.setText(s(p.getNotes()).isBlank()?"No notes":p.getNotes());if(lblDetailAttachment!=null){int purchaseId=p.getId();lblDetailAttachment.setText("Loading attachments…");UiTaskExecutor.submitLatest("purchase-register-attachment-count",()->support.documentAttachments("PURCHASE",purchaseId).size(),count->{if(selected!=null&&selected.getId()==purchaseId)lblDetailAttachment.setText(AttachmentPreviewSupport.countLabel(count));},failure->{if(selected!=null&&selected.getId()==purchaseId)lblDetailAttachment.setText(s(p.getAttachmentPath()).isBlank()?"No attachments":"1 attachment");});}if(lblDetailBillingAddress!=null)lblDetailBillingAddress.setText(s(p.getBillingAddress()).isBlank()?"Not set":p.getBillingAddress());if(lblDetailDeliveryAddress!=null){String delivery=s(p.getDeliveryAddress());if(delivery.isBlank()&&p.isSameAsBilling())delivery=s(p.getBillingAddress());lblDetailDeliveryAddress.setText(delivery.isBlank()?"Not set":delivery);}boolean pendingApproval=isPendingApproval(p),admin=isAdmin();if(approvalActionBox!=null){approvalActionBox.setManaged(pendingApproval&&admin);approvalActionBox.setVisible(pendingApproval&&admin);}if(btnApprovePurchase!=null)btnApprovePurchase.setDisable(!pendingApproval||!admin);if(btnRejectPurchase!=null)btnRejectPurchase.setDisable(!pendingApproval||!admin);}
private void configureDetailsCloseButton(){
     if(btnCloseDetails == null) return;
     btnCloseDetails.setGraphic(IconFactory.icon("cancel"));
     btnCloseDetails.getProperties().put("erp-icon-preserve", true);
     btnCloseDetails.setAccessibleText("Close purchase details");
 }
 @FXML private void closeDetails(){selected=null;RegisterUiSupport.hideDrawer(detailDrawer,mainSplit,tablePurchase);}
 @FXML private void approveSelectedPurchase(){Purchase p=selected;if(p==null)return;try{service.approve(p.getInvoiceNo());NotificationService.add("Purchase "+p.getInvoiceNo()+" approved.");refresh();}catch(Exception e){error(e);}}
 @FXML private void rejectSelectedPurchase(){Purchase p=selected;if(p==null)return;OwnedTextInputDialog dialog=new OwnedTextInputDialog();dialog.setTitle("Reject Purchase");dialog.setHeaderText("Reject "+p.getInvoiceNo());dialog.setContentText("Reason:");dialog.showAndWait().map(String::trim).filter(v->!v.isBlank()).ifPresent(reason->{try{service.reject(p.getInvoiceNo(),reason);NotificationService.add("Purchase "+p.getInvoiceNo()+" rejected.");refresh();}catch(Exception e){error(e);}});}
 @FXML private void paySelected(){if(selected!=null)payment(selected);}
 @FXML private void editSelected(){if(selected!=null)edit(selected);}
 @FXML private void excelSelected(){if(selected!=null)excel(selected);}
 @FXML private void emailSelected(){if(selected!=null)email(selected);}
 private void payment(Purchase p){
  if(p==null)return;
  String document=s(p.getDocumentStatus()).trim().toUpperCase(java.util.Locale.ROOT);
  if(java.util.Set.of("PENDING APPROVAL","REJECTED").contains(document)){info("Admin approval is required before opening payments for this Purchase.");return;}
  if("DRAFT".equals(document)){info("Post the Purchase before opening payments.");return;}
  if(java.util.Set.of("DELETED","CANCELLED").contains(document)){info("Payments are unavailable for deleted or cancelled purchases.");return;}
  if(s(p.getInvoiceNo()).isBlank()){info("Unable to open Record Payment because the selected purchase invoice is not available.");return;}
  try{
   PurchaseScreenContext.select(p.getInvoiceNo());
   FXMLLoader loader=new FXMLLoader(org.example.util.ResourceLocator.require("/fxml/pages/PurchasePayment.fxml"));
   Parent root=loader.load();
   org.example.util.ProfessionalUiEnhancer.enhance(root);
   PurchasePaymentController controller=loader.getController();
   controller.loadPurchase(p);
   NavigationManager.getInstance().showPreparedPage("/fxml/pages/PurchasePayment.fxml",root,controller);
  }catch(Exception e){error(e);}
 }
 private void pdf(Purchase p){try{Purchase full=requirePurchase(p);java.awt.Desktop.getDesktop().open(ManagedInvoicePdfService.purchase(full).toFile());}catch(Exception e){error(e);}}
 private void excel(Purchase p){try{Purchase full=requirePurchase(p);Path file=ExcelOutputService.purchase(full);if(java.awt.Desktop.isDesktopSupported())java.awt.Desktop.getDesktop().open(file.toFile());else info("Excel file created: "+file);}catch(Exception e){error(e);}}
 private void email(Purchase p){if(isApprovalLocked(p)){info("Approve this Purchase before sending it externally.");return;}String stage="loading the purchase invoice";String recipient="";try{Purchase full=requirePurchase(p);if(full.getSupplier()==null)throw new IllegalStateException("No supplier is linked to "+full.getInvoiceNo()+".");recipient=s(full.getSupplier().getEmail()).trim();if(recipient.isBlank())throw new IllegalStateException("Supplier email is missing for "+full.getSupplier().getName()+". Update Supplier Master and try again.");stage="generating the purchase invoice PDF";Path pdf=ManagedInvoicePdfService.purchase(full);stage="sending the email";EmailService.send(recipient,"Purchase Invoice "+full.getInvoiceNo(),"Dear "+s(full.getSupplier().getName())+",\n\nPlease find the purchase invoice attached.\n\nRegards,\n"+org.example.config.ConfigManager.get("company.name","DSE ERP"),pdf);service.markEmailSent(full.getId());logCommunication(full.getId(),recipient,"Purchase Invoice "+full.getInvoiceNo(),"SENT",null);refresh();info("Email sent successfully to "+recipient+".");}catch(Exception failure){logCommunication(p.getId(),recipient,"Purchase Invoice "+p.getInvoiceNo(),"FAILED",stage+": "+rootMessage(failure));error(new IllegalStateException("Email failed while "+stage+".\n\n"+rootMessage(failure),failure));}}
 private Purchase requirePurchase(Purchase row){Purchase full=service.getByInvoice(row.getInvoiceNo());if(full==null)throw new IllegalStateException("Purchase invoice "+row.getInvoiceNo()+" was not found. Refresh the register and try again.");return full;}
 private void logCommunication(int id,String recipient,String subject,String status,String error){try{support.communication(new SupportApiClient.CommunicationRequest("PURCHASE",id,"EMAIL",recipient,subject,status,error,user()));}catch(Exception ignored){}}
 private String rootMessage(Throwable failure){Throwable root=failure;while(root.getCause()!=null)root=root.getCause();String message=root.getMessage();return message==null||message.isBlank()?root.getClass().getSimpleName():message;}
 private void whatsapp(Purchase p){if(isApprovalLocked(p)){info("Approve this Purchase before sharing it externally.");return;}try{Purchase full=service.getByInvoice(p.getInvoiceNo());String phone=s(full.getSupplier().getPhone()).replaceAll("\\D","");if(phone.length()==10)phone="91"+phone;if(phone.isBlank())throw new IllegalStateException("Supplier mobile number is missing. Update it in Supplier Master.");Path pdf=ManagedInvoicePdfService.purchase(full);WhatsappService.openWhatsappWithMessage(phone,PaymentMessageService.purchaseMessage(full),pdf,PaymentMessageService.configuredQrPath());support.communication(new SupportApiClient.CommunicationRequest("PURCHASE",full.getId(),"WHATSAPP",phone,"Purchase Invoice "+full.getInvoiceNo(),"SENT",null,user()));refresh();info("WhatsApp is ready. The purchase PDF and configured UPI QR are on the clipboard for attachment.");}catch(Exception e){error(e);}}
 private void delete(Purchase p){if(isFinanciallyLocked(p)){info("Paid, partially paid, or settled purchases cannot be deleted. Create a purchase return instead.");return;}String status=s(p.getDocumentStatus()).trim().toUpperCase(java.util.Locale.ROOT);if("DELETED".equals(status)){info("This purchase is already marked as deleted.");return;}String prompt="Delete "+p.getInvoiceNo()+"?\n\nThe document will disappear from the normal Purchase Register, but its backend audit record will be retained as DELETED.\nAny inventory movement already posted by this purchase will be reversed safely.";if(new OwnedAlert(Alert.AlertType.CONFIRMATION,prompt,ButtonType.YES,ButtonType.NO).showAndWait().orElse(ButtonType.NO)!=ButtonType.YES)return;try{service.delete(p.getInvoiceNo());refresh();closeDetails();info(p.getInvoiceNo()+" deleted from the register. Backend audit record retained.");}catch(Exception e){error(e);}}
 private void duplicate(Purchase p){try{FXMLLoader l=new FXMLLoader(org.example.util.ResourceLocator.require("/fxml/pages/Purchase.fxml"));Parent root=l.load();org.example.util.ProfessionalUiEnhancer.enhance(root);PurchaseController c=l.getController();c.loadPurchase(service.getByInvoice(p.getInvoiceNo()));c.prepareDuplicate();NavigationManager.getInstance().showPreparedPage("/fxml/pages/Purchase.fxml",root,c);}catch(Exception e){error(e);}}

 private void notes(Purchase p){TextInputDialog d=new OwnedTextInputDialog(p.getNotes());d.setHeaderText("Notes / Remarks • "+p.getInvoiceNo());d.showAndWait().ifPresent(v->{try{support.notes("PURCHASE",p.getId(),v);refresh();}catch(Exception e){error(e);}});}
 private void cancelPurchase(Purchase p){if(p==null)return;String status=s(p.getDocumentStatus()).trim().toUpperCase(java.util.Locale.ROOT);if("CANCELLED".equals(status)){info("This purchase is already cancelled.");return;}if("DELETED".equals(status)){info("Deleted purchases cannot be cancelled.");return;}if(isFinanciallyLocked(p)){info("Paid, partially paid, or settled purchases cannot be cancelled. Create a purchase return instead.");return;}String prompt="Cancel "+p.getInvoiceNo()+"?\n\nThe document will remain visible with status CANCELLED and any posted inventory will be reversed safely.";if(new OwnedAlert(Alert.AlertType.CONFIRMATION,prompt,ButtonType.YES,ButtonType.NO).showAndWait().orElse(ButtonType.NO)!=ButtonType.YES)return;try{service.cancel(p.getInvoiceNo());refresh();closeDetails();info(p.getInvoiceNo()+" cancelled. The document remains visible in the register.");}catch(Exception e){error(e);}}
 private void createReturn(Purchase p){String document=s(p.getDocumentStatus()).toUpperCase(java.util.Locale.ROOT);if(java.util.Set.of("PENDING APPROVAL","REJECTED").contains(document)){info("Admin approval is required before creating a Purchase Return.");return;}if("DRAFT".equals(document)){info("Post the Purchase before creating a Purchase Return.");return;}if("DELETED".equals(document)||"CANCELLED".equals(document)){info("Deleted or cancelled purchases cannot create a Purchase Return.");return;}Purchase full=service.getByInvoice(p.getInvoiceNo());if(full==null){info("Purchase invoice not found. Refresh and try again.");return;}List<ReturnEditorService.InvoiceItem> items=full.getLines().stream().map(line->new ReturnEditorService.InvoiceItem(line.getItemCode(),line.getItemDescription(),line.getQuantity(),line.getRate(),line.getDiscountPercent(),line.getGstPercent())).toList();ReturnEditorService.show(tablePurchase.getScene().getWindow(),ReturnEditorService.Type.PURCHASE,p.getInvoiceNo(),p.getSupplier().getName(),p.getSupplier().getId(),items).ifPresent(no->{refresh();info("Purchase return created: "+no);});}
 @FXML private void exportPurchase(){org.example.service.PermissionService.require("PURCHASE.EXPORT", "Export Purchases");File f=choose("Purchase_Register.xlsx","Excel","*.xlsx");if(f==null)return;String supplier=cmbSupplier.getValue();if(supplier!=null&&supplier.startsWith("All"))supplier="";String selectedSupplier=supplier,q=txtSearch.getText(),payment=cmbPaymentStatus.getValue(),dueFilter="All",mail=cmbMailStatus.getValue(),documentStatus=cmbDocumentStatus.getValue(),returnStatus=cmbReturnStatus.getValue();LocalDate from=dpFrom.getValue(),to=dpTo.getValue();UiTaskExecutor.submitAction("purchase-register-export-excel",()->{List<Purchase> rows=service.allFiltered(q,selectedSupplier,from,to,payment,dueFilter,mail,documentStatus,returnStatus);writePurchaseExcel(f,rows);return rows.size();},count->info("Purchase register exported • "+count+" records."),this::error);}
 @FXML private void exportPdf(){org.example.service.PermissionService.require("PURCHASE.EXPORT", "Export Purchases");File f=choose("Purchase_Register.pdf","PDF","*.pdf");if(f==null)return;String supplier=cmbSupplier.getValue();if(supplier!=null&&supplier.startsWith("All"))supplier="";String selectedSupplier=supplier,q=txtSearch.getText(),payment=cmbPaymentStatus.getValue(),dueFilter="All",mail=cmbMailStatus.getValue(),documentStatus=cmbDocumentStatus.getValue(),returnStatus=cmbReturnStatus.getValue();LocalDate from=dpFrom.getValue(),to=dpTo.getValue();UiTaskExecutor.submitAction("purchase-register-export-pdf",()->{List<Purchase> rows=service.allFiltered(q,selectedSupplier,from,to,payment,dueFilter,mail,documentStatus,returnStatus);org.example.service.BrandedRegisterPdfService.export(f.toPath(),"Purchase Register",new String[]{"Invoice","Date","Supplier","Amount","Paid","Pending","Due","Document Status","Payment Status"},rows.stream().map(x->new String[]{x.getInvoiceNo(),BusinessClock.formatDate(x.getInvoiceDate()),x.getSupplier()==null?"":s(x.getSupplier().getName()),exportMoney(x.getTotalAmount()),exportMoney(x.getPaidAmount()),exportMoney(x.getBalanceAmount()),due(x),documentStatus(x),paymentStatusDisplay(x)}).toList(),new float[]{2,1.3f,2.4f,1.3f,1.3f,1.3f,1.5f,1.5f,1.6f});return rows.size();},count->info("Purchase register PDF exported • "+count+" records."),this::error);}
 private void writePurchaseExcel(File f,List<Purchase> rows)throws Exception{try(Workbook w=new XSSFWorkbook();FileOutputStream o=new FileOutputStream(f)){Sheet sh=w.createSheet("Purchases");String[]h={"Invoice","Date","Supplier","Amount","Paid","Pending","Due","Document Status","Payment Status","Email"};Row r=sh.createRow(0);for(int i=0;i<h.length;i++)r.createCell(i).setCellValue(h[i]);int n=1;for(Purchase x:rows){r=sh.createRow(n++);String[]v={x.getInvoiceNo(),BusinessClock.formatDate(x.getInvoiceDate()),x.getSupplier()==null?"":s(x.getSupplier().getName()),String.valueOf(x.getTotalAmount()),String.valueOf(x.getPaidAmount()),String.valueOf(x.getBalanceAmount()),due(x),documentStatus(x),paymentStatusDisplay(x),x.isEmailSent()?"Sent":"Not Sent"};for(int i=0;i<v.length;i++)r.createCell(i).setCellValue(v[i]);}w.write(o);}}
 private String exportMoney(double value){return NumberFormat.getCurrencyInstance(Locale.of("en","IN")).format(value).replace("₹","₹ ");}


 private boolean isAdmin(){return SessionService.isAdmin();}
 private boolean isPendingApproval(Purchase p){return p!=null&&"PENDING APPROVAL".equalsIgnoreCase(s(p.getDocumentStatus()).trim());}
 private boolean isApprovalLocked(Purchase p){if(p==null)return false;String status=s(p.getDocumentStatus()).trim().toUpperCase(java.util.Locale.ROOT);return status.equals("PENDING APPROVAL")||status.equals("REJECTED");}
 private String paymentStatusDisplay(Purchase p){if(p==null)return"";String status=s(p.getPaymentStatus()).trim().toUpperCase(java.util.Locale.ROOT);if("RETURN APPROVAL PENDING".equals(status))return"Return Pending Approval";if("RETURN PAID".equals(status))return"Return Paid";if("RETURN PARTIAL".equals(status))return"Return Partial";if("RETURN PENDING".equals(status))return"Return Pending";return status;}
 private boolean isFinanciallyLocked(Purchase p){if(p==null)return false;String payment=s(p.getBasePaymentStatus()).toUpperCase(java.util.Locale.ROOT);return p.getPaidAmount()>.009||payment.contains("PAID")||payment.contains("SETTLED")||payment.contains("PARTIAL");}
 private boolean isFullyPaid(Purchase p){if(p==null)return false;String payment=s(p.getBasePaymentStatus()).trim().toUpperCase(java.util.Locale.ROOT);return payment.equals("PAID")||payment.equals("SETTLED")||p.getPaidAmount()+.009>=p.getTotalAmount();}
 private boolean isReturnEligible(Purchase p){if(p==null||!isFullyPaid(p))return false;String document=s(p.getDocumentStatus()).trim().toUpperCase(java.util.Locale.ROOT);String current=s(p.getPaymentStatus()).trim().toUpperCase(java.util.Locale.ROOT);return "APPROVED".equals(document)&&!java.util.Set.of("RETURN APPROVAL PENDING","RETURN PENDING","RETURN PARTIAL").contains(current);}
 private boolean isActiveFinancialDocument(Purchase p){String document=s(p.getDocumentStatus()).toUpperCase(java.util.Locale.ROOT);return !document.contains("CANCEL")&&!document.contains("DELETE")&&!"DRAFT".equals(document);}
 private File choose(String n,String l,String x){FileChooser c=new FileChooser();c.setInitialFileName(n);try{Path folder=org.example.config.WorkspaceStorageManager.reportFolder("Purchase",dpFrom.getValue());if(Files.isDirectory(folder))c.setInitialDirectory(folder.toFile());}catch(Exception ignored){}c.getExtensionFilters().add(new FileChooser.ExtensionFilter(l,x));return c.showSaveDialog(tablePurchase.getScene().getWindow());}private String due(Purchase p){String document=s(p.getDocumentStatus()).toUpperCase(java.util.Locale.ROOT);if(document.contains("DELETE"))return"Deleted";if(document.contains("CANCEL"))return"Cancelled";String status=s(p.getPaymentStatus()).toUpperCase(java.util.Locale.ROOT);if("RETURN APPROVAL PENDING".equals(status))return"Awaiting Approval";if("RETURN PAID".equals(status))return"Settled";if("RETURN PENDING".equals(status)||"RETURN PARTIAL".equals(status)){LocalDate due=p.getReturnDueDate();if(due==null)return"Return Due";long d=java.time.temporal.ChronoUnit.DAYS.between(BusinessClock.today(),due);return d<0?"Return Overdue by "+Math.abs(d)+" days":d==0?"Return Due Today":"Return Due in "+d+" Days";}if(p.getBalanceAmount()<=.01)return"Settled";if(p.getDueDate()==null)return"Not set";long d=java.time.temporal.ChronoUnit.DAYS.between(BusinessClock.today(),p.getDueDate());return d<0?"Overdue by "+Math.abs(d)+" days":d==0?"Due today":"Due in "+d+" days";}private String fmt(double v){return money.format(v).replace("₹","₹ ");}private boolean filterEventsSuppressed(){return applyingSavedView||suppressFilterEvents;} private void batchFilterUpdate(Runnable update){boolean previous=suppressFilterEvents;suppressFilterEvents=true;try{update.run();}finally{suppressFilterEvents=previous;}} private String s(String v){return v==null?"":v;}private String user(){return SessionService.current()==null?"System":SessionService.current().getFullName();}private void info(String m){org.example.util.ToastManager.success(tablePurchase,"Completed",m);}private void error(Throwable e){e.printStackTrace();String message=e.getMessage()==null?"Operation failed":e.getMessage();org.example.util.ModernDialog.error(tablePurchase,"Operation failed","Something went wrong",message);}


    private void applyRangeButtonIcons() {
        applyIcon(btnAllDatesRange,"calendar"); applyIcon(btnTodayRange,"calendar"); applyIcon(btnYesterdayRange,"history");
        applyIcon(btnSevenDaysRange,"calendar"); applyIcon(btnThirtyDaysRange,"calendar"); applyIcon(btnCustomRange,"calendar");
    }
    private void applyIcon(ButtonBase b,String semantic){ if(b!=null){b.setGraphic(IconFactory.compactIcon(semantic,15));b.getProperties().put("erp-icon-preserve",true);} }
 @Override public void onScreenShown(boolean reusedFromCache){org.example.shortcut.ShortcutRegistry.bindLabel(btnNewPurchase,org.example.shortcut.ShortcutRegistry.Action.NEW_PURCHASE,"Create Purchase");org.example.util.OperationalUiSupport.focusWorkArea(tablePurchase);if(ImportViewContext.consume("Purchases")){batchFilterUpdate(()->{dpFrom.setValue(BusinessClock.today().minusYears(20));dpTo.setValue(BusinessClock.today());});pageState.reset();reloadPage(true);return;}if(!reusedFromCache)return;if(ScreenRefreshPolicy.shouldRefresh("purchase-register",ScreenRefreshPolicy.Mode.WHEN_STALE))refresh();}
 @Override public void onScreenHidden(){UiTaskExecutor.cancelPrefix("purchase-");searchDebouncer.cancel();}
}
